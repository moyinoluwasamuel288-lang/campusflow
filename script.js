// saveNote function
function saveNote() {
    // Functionality to save a note
}

// loadNote event listener
document.getElementById('loadButton').addEventListener('click', loadNote);

// changeFont function
function changeFont(font) {
    // Functionality to change font
}
